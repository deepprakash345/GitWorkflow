{
  "adaptiveform": "0.10.0",
  "metadata": {
    "grammar": "json-formula-1.0.0",
    "version": "1.0.0"
  },
  "items": [
    {
      "name": "textInput",
      "type": "string",
      "fieldType": "text-input",
      "required": true,
      "pattern": "^[a-zA-Z. ]{1,10}$",
      "maxLength": 11,
      "minLength": 3,
      "label": {
        "value": "Constraints for String"
      },
      "description" : "The Field has custom constraint messages",
      "constraintMessages": {
        "required": "required field",
        "pattern": "only characters, dot (.) and space",
        "maxLength": "maximum 11 characters",
        "minLength": "minimum 3 characters"
      }
    }
  ]
}