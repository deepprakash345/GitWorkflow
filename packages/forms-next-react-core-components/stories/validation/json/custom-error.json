{
    "items": [
        {
            "name": "textInput",
            "type": "string",
            "viewType": "text-input",
            "required": true,
            "pattern": "^[a-zA-Z. ]{1,10}$",
            "maxLength": 11,
            "minLength": 0,
            "label": {
                "value": "Text input"
            },
            "constraintMessages": {
                "required": "The field is required",
                "pattern": "pattern mismatch",
                "maxLength": "length is maximum",
                "minLength": "length is minimum"
            }
        },
        {
            "name": "numberInput",
            "type": "number",
            "viewType": "number-input",
            "required": true,
            "maximum": 100,
            "minimum": 0,
            "label": {
                "value": "Number input"
            },
            "constraintMessages": {
                "required": "The field is required",
                "maximum": "value is maximum",
                "minimum": "value is minimum"
            }
        }
    ]
}