// this is added because React-Spectrum uses static assets which is not supported OOTB in jest.
// We may need to handle that once we start testing styling.
//https://jestjs.io/docs/webpack#handling-static-assets
module.exports = {};