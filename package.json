{
  "name": "root",
  "private": true,
  "scripts" : {
    "clean" : "lerna clean --yes && rm -rf packages/*/lib packages/*/build",
    "build" : "lerna run build",
    "build:libs" : "lerna run build-libs",
    "start" : "lerna run start",
    "story" : "lerna run storybook",
    "test" : "lerna run test",
    "bootstrap" : "npx lerna bootstrap",
    "docs" : "lerna run docs"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
