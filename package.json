{
  "name": "root",
  "private": true,
  "scripts" : {
    "clean" : "lerna clean && rm -rf node_modules packages/*/lib packages/*/node_modules packages/*/build",
    "build" : "lerna run build",
    "start" : "lerna run start",
    "story" : "lerna run storybook",
    "test" : "lerna run test",
    "bootstrap" : "npx lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
